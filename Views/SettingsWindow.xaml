<Window x:Class="FolderIconManager.WPF.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FolderIconManager.WPF.ViewModels"
        mc:Ignorable="d"
        Title="⚙️ تنظیمات" 
        Height="400" 
        Width="600"
        FlowDirection="RightToLeft"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="NoResize">

    <Window.DataContext>
        <vm:SettingsViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- هدر -->
        <Border Grid.Row="0" 
                Background="White" 
                CornerRadius="8" 
                Padding="15" 
                Margin="0,0,0,15"
                Effect="{StaticResource MaterialDesignShadowDepth1}">
            <StackPanel>
                <TextBlock Text="⚙️ تنظیمات برنامه" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="#2C3E50"
                           Margin="0,0,0,5"/>
                <TextBlock Text="تنظیمات مربوط به دانلود و اعمال ایکون‌ها را مدیریت کنید" 
                           FontSize="12" 
                           Foreground="#7F8C8D"/>
            </StackPanel>
        </Border>

        <!-- تنظیمات اصلی -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                Padding="20"
                Effect="{StaticResource MaterialDesignShadowDepth1}">
            <StackPanel>
                <!-- مسیر دانلود ایکون‌ها -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="📁 مسیر دانلود ایکون‌ها" 
                                   FontSize="14" 
                                   FontWeight="Bold" 
                                   Foreground="#2C3E50"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="پوشه‌ای که ایکون‌های دانلود شده در آن ذخیره می‌شوند" 
                                   FontSize="12" 
                                   Foreground="#7F8C8D"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1" 
                            Content="📂 انتخاب پوشه" 
                            Command="{Binding SelectIconPathCommand}"
                            Padding="15,8"
                            Background="#3498DB"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="12"
                            Margin="10,0,0,0"/>
                </Grid>

                <TextBox Text="{Binding CurrentSettings.IconDownloadPath}" 
                         FontSize="12"
                         Padding="10,8"
                         BorderBrush="#E0E0E0"
                         BorderThickness="1"
                         Margin="0,0,0,20"
                         IsReadOnly="True"
                         Background="#F8F9FA"/>

                <!-- گزینه‌های دیگر -->
                <TextBlock Text="🔧 تنظیمات دیگر" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Foreground="#2C3E50"
                           Margin="0,0,0,10"/>

                <CheckBox IsChecked="{Binding CurrentSettings.AutoApplyIcons}" 
                          Content="اعمال خودکار ایکون پس از دانلود"
                          FontSize="13"
                          Foreground="#34495E"
                          Margin="0,0,0,10"/>

                <CheckBox IsChecked="{Binding CurrentSettings.BackupOriginal}" 
                          Content="پشتیبان‌گیری از ایکون اصلی قبل از جایگزینی"
                          FontSize="13"
                          Foreground="#34495E"
                          Margin="0,0,0,10"/>

                <CheckBox IsChecked="{Binding CurrentSettings.ShowNotifications}" 
                          Content="نمایش اعلان‌های سیستم پس از اعمال ایکون"
                          FontSize="13"
                          Foreground="#34495E"
                          Margin="0,0,0,20"/>

                <!-- دکمه باز کردن پوشه -->
                <Button Content="📂 باز کردن پوشه دانلود" 
                        Command="{Binding OpenIconsFolderCommand}"
                        Padding="15,8"
                        Background="#95A5A6"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="12"
                        HorizontalAlignment="Left"
                        Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- دکمه‌های پایین -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    FlowDirection="LeftToRight"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="💾 ذخیره" 
                    Command="{Binding SaveSettingsCommand}"
                    Padding="20,10"
                    Background="#27AE60"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="Bold"
                    Margin="0,0,10,0"/>
            
            <Button Content="❌ انصراف" 
                    Command="{Binding CancelCommand}"
                    Padding="20,10"
                    Background="#E74C3C"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    Margin="0,0,10,0"/>
            
            <Button Content="🔄 بازنشانی به پیش‌فرض" 
                    Command="{Binding ResetToDefaultsCommand}"
                    Padding="20,10"
                    Background="#95A5A6"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
